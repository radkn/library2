package com.lib.training.model.repository;

import com.lib.training.model.entities.BookUnit;
import org.springframework.data.repository.CrudRepository;

public interface BookUnitRepository extends CrudRepository<BookUnit, Long> {
}
